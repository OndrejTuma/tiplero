{% if url %}
    <a class="button{{ outline ? " button--outline" }}{{ class ? " " ~ class }}" href="{{ url }}">
        {% if icon %}
            {% include "../../../build/svg/" ~ icon ~ ".svg" %}
        {% endif %}
        {{ label }}
    </a>
{% else %}
    <button class="button{{ outline ? " button--outline" }}{{ class ? " " ~ class }}" type="submit">
        {% if icon %}
            {% include "../../../build/svg/" ~ icon ~ ".svg" %}
        {% endif %}
        {{ label }}
    </button>
{% endif %}